const JS_FILES = '**/*.?([cm])js?(x)';
const TS_FILES = '**/*.?([cm])ts?(x)';

const JSON_FILES = '**/*.json?([c5])';
const YAML_FILES = '**/*.y?(a)ml';

const GLOB_IGNORES = '**/*.?(spec|test|bench|bencmark).?([cm])[jt]s?(x)';

module.exports = {
  JS_FILES,
  TS_FILES,
  JSON_FILES,
  YAML_FILES,
  GLOB_IGNORES,
};
